{
  "name": "action-mktemp-repo",
  "version": "1.0.0",
  "description": "a GitHub Action to make temporary repositories to be used in particular for testing workflows",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "lint-fix": "eslint . --fix",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cawfeecake/action-mktemp-repo.git"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "author": "cawfeecake",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/cawfeecake/action-mktemp-repo/issues"
  },
  "homepage": "https://github.com/cawfeecake/action-mktemp-repo#readme",
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "eslint": "^8.37.0",
    "jest": "^29.5.0"
  }
}
